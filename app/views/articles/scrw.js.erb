$(document).ready(function() {
   $('#mycart .remove').click(function(e) {
    var $this, url;
    e.preventDefault();
    $this =  $(this).closest('a');
    url = $this.data('targeturl');
     $.ajax({
        url: url,
        type: 'put'
    });
  });
});